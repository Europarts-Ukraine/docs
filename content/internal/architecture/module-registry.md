# Module Registry

The module registry describes how backend resources become frontend modules, navigation entries, tables, forms, relations, and history views.

The long-term direction is to reduce duplicated backend knowledge in frontend static module definitions and make backend metadata the source of truth where appropriate.

## To Document

- module key conventions;
- system modules;
- custom entity modules;
- fields and widgets;
- relation metadata;
- RBAC integration;
- fallback behavior when registry metadata is unavailable.

