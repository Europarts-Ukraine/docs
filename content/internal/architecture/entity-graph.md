# Entity Graph

The entity graph exposes metadata about available entities, fields, relations, and capabilities.

It is used by modules that need to reason about platform entities generically, such as exports, imports, custom entities, and future backend-driven UI.

## To Document

- entity identifiers;
- field metadata;
- relation metadata;
- capability registration;
- custom entity integration;
- consumers of the graph.

